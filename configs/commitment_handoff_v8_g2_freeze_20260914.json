{
  "protocol": "COMMITMENT-HANDOFF-3D-V8-PLAIN-MAPPO-G2-DEVELOPMENT-V1",
  "artifact_class": "DEVELOPMENT_ONLY_TASK_LEARNABILITY_GATE",
  "paper_evidence": false,
  "purpose": "Test branch-value learnability after V7 showed that the uniformly correct early reservation action caused binary-policy collapse.",
  "frozen_task": {
    "service_envelope_mode": "compositional_v6_staged",
    "commitment_decision_mode": "branch_value_v8",
    "authorization_start_step": 0,
    "authorization_deadline": 16,
    "authorization_decision": "compulsory_retain",
    "branch_step": 24,
    "commitment_action_repeat": 8,
    "future_corridor_lateral_offset": 900.0,
    "handoff_safety_mode": "blue_team_only",
    "handoff_corridor_radius": 900.0,
    "authorization_hold_steps": 8,
    "refresh_hold_steps": 16,
    "max_steps": 260
  },
  "g0_basis": {
    "script": "scripts/audit_compositional_service_envelope_handoff_v8_g0.py",
    "required_verdict": "V8_G0_BRANCH_VALUE_PASS",
    "required_controller_order": {
      "current_profiles": "always_retain > always_reconstruct",
      "future_profiles": "always_reconstruct > always_retain",
      "all_profiles": "public_value_rule succeeds using only public envelope values"
    }
  },
  "plain_baseline": {
    "algorithm": "MLP MAPPO",
    "graph_encoder": "no_graph",
    "hidden_dim": 64,
    "actor_action_mask_mode": "relay_decision_only",
    "only_causal_relay_actions": ["branch"],
    "no_candidate_representation": true,
    "no_sampler": true,
    "no_auxiliary_loss": true
  },
  "development_budget": {
    "training_seeds": [83911, 83912, 83913],
    "updates": 64,
    "num_envs": 4,
    "rollout_steps": 64,
    "endpoint_episodes_per_profile": 60
  },
  "g2_pass_rule": {
    "future_success_interval": [0.1, 0.9],
    "future_profiles_need_at_least_two_of_three_moderate_seeds": true,
    "current_profiles_need_positive_mean_success": true,
    "current_profiles_need_retain_at_branch_in_at_least_two_seeds": true,
    "future_profiles_need_reconstruct_at_branch_in_at_least_two_seeds": true
  },
  "stop_rule": "If G2 fails, diagnose one remaining structural cause before any new task modification; do not sweep V8 parameters."
}
