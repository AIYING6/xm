{
  "method": "IPPO",
  "paper_role": "decentralized non-CTDE auxiliary baseline",
  "implementation_status": "lower_priority_than_HAPPO",
  "stop_rule": "Use only if it can be produced from the existing MAPPO path without delaying the HAPPO attempt or main baseline training.",
  "fairness_requirements": [
    "same actor observation",
    "no centralized critic information during value learning if implemented as true IPPO",
    "same final test episodes"
  ]
}
