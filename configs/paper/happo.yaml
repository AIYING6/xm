{
  "method": "HAPPO-style",
  "paper_role": "priority heterogeneous sequential PPO-style external baseline attempt for Q1 credibility",
  "implementation_status": "training_and_checkpoint_sweep_smoke_passed_pending_dev_1m",
  "trainer_script": "scripts/train_happo_baseline.py",
  "graph_encoder": "no_graph",
  "hidden_dim": 64,
  "role_dim": 8,
  "intent_dim": 8,
  "stop_rule": "Do not report this implementation as standard HAPPO unless the original HAPPO objective and update correction are implemented or a trusted public implementation is integrated. Otherwise report it as HAPPO-style / heterogeneous sequential PPO.",
  "current_limitations": [
    "minimal no-graph HAPPO-style external baseline",
    "per-agent actor/critic modules with sequential PPO updates",
    "does not claim full standard HAPPO objective equivalence",
    "training smoke passed",
    "validation/test checkpoint-sweep smoke passed",
    "not yet run at 1M-step development budget"
  ],
  "fairness_requirements": [
    "same scenario",
    "same training budget B_star",
    "same validation checkpoint selection rule",
    "same final test episodes",
    "same BC data if BC initialization is used",
    "same reward and safety constraints"
  ]
}
