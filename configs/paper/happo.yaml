{
  "method": "HAPPO",
  "paper_role": "priority heterogeneous sequential PPO external baseline for Q1 credibility",
  "implementation_status": "standard_sequential_joint_ratio_update_implemented_pending_smoke_and_dev_1m",
  "trainer_script": "scripts/train_happo_baseline.py",
  "output_method_name": "happo_standard",
  "graph_encoder": "no_graph",
  "hidden_dim": 64,
  "role_dim": 8,
  "intent_dim": 8,
  "stop_rule": "Report as HAPPO only for runs produced after the sequential joint-ratio correction was implemented. Earlier HAPPO-style development runs are historical diagnostics and must not be mixed into formal paper evidence.",
  "current_limitations": [
    "no-graph HAPPO external baseline",
    "per-agent actor/centralized-critic modules",
    "sequential policy update uses previous-agent joint-ratio correction",
    "post-correction training smoke pending",
    "post-correction validation/test checkpoint-sweep smoke pending",
    "not yet run at 1M-step development budget"
  ],
  "fairness_requirements": [
    "same scenario",
    "same training budget B_star",
    "same validation checkpoint selection rule",
    "same final test episodes",
    "same BC data if BC initialization is used",
    "same reward and safety constraints"
  ]
}
