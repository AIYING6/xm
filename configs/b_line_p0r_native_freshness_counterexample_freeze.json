{
  "protocol": "B-LINE-P0R-NATIVE-FRESHNESS-COUNTEREXAMPLE-V1",
  "purpose": "Establish or reject a native freshness paired counterexample before any deterministic solver is designed.",
  "environment": {
    "module": "envs/redundant_topology_uav_env.py",
    "scale": "main",
    "configuration_rule": "Use the unmodified scale_config('main') default, including native tau_max=5 and comm_dropout=0.0."
  },
  "fixed_horizon_steps": 7,
  "objective_action": 1,
  "histories": {
    "fresh": "All agents idle through step 6; scouts sense objective action 1 at step 7; terminals remain idle.",
    "stale": "Scouts sense objective action 1 at step 1; all agents idle through step 7; terminals remain idle."
  },
  "required_equal_current_physical_state": [
    "step_count",
    "positions",
    "objective_positions",
    "task_adjacency",
    "active_adjacency",
    "objective_progress",
    "completed",
    "roles",
    "scout_assignment",
    "terminal_assignment"
  ],
  "allowed_difference": [
    "cache freshness generated by the legal sensing-time history",
    "freshness-dependent actor observation fields and legal action masks"
  ],
  "prohibitions": [
    "cache threshold override",
    "environment mutation",
    "reward or termination change",
    "failure-semantic change",
    "new action",
    "solver",
    "PPO or RL training",
    "evaluation tape",
    "checkpoint selection"
  ],
  "verdicts": {
    "go": "B_P0R_GO",
    "conditional": "B_P0R_CONDITIONAL",
    "no_go": "B_P0R_NO_GO"
  },
  "training_started": false,
  "evaluation_started": false,
  "automatic_continuation": false
}
