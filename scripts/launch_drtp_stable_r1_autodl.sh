#!/usr/bin/env bash
# Deliberately inert until a separately authorized R1 execution package adds the runner.
set -euo pipefail
echo "R1 readiness package only: no training is authorized by this script."
echo "Obtain a separately frozen R1 execution package after human authorization."
exit 64
