# P3-A.3 Implementation Smoke Invalidation (2026-08-08)

- status: **IMPLEMENTATION SMOKE ONLY — INVALID FOR SCIENTIFIC ANALYSIS**
- scope: `full_ea_rg s0 G1` 100 episodes generated by a CPU smoke call of
  `run_p3a_ood_eval.run_cell(...)` (device=cpu, eval_batch_size=2) BEFORE the
  formal 8400-episode run.
- What was inspected: one episode had `recovery_event_time=21`; used only to
  confirm the raw-row schema and recovery clock plumb through end-to-end.

## Boundary

- NOT a protocol failure; the smoke used the frozen v1.1.3 evaluator and P1
  recovery clock, but it was run on CPU at batch=2 outside the formal schedule.
- These 100 rows were **not written to any formal raw CSV** (the smoke only
  returned rows in memory; `run_p3a_ood_eval.py` now has a fail-if-exists guard
  so the formal output file can never be overwritten/appended).

## Consequence

- The 100 smoke episodes are excluded from P3-A.3.
- Formal 8400-episode raw collection starts fresh from `p3a-ood-collection-impl-v1.0`
  exact-tag clean worktree with an empty output directory.
- No scientific result may be derived from this smoke run.
