# PAPER-Q2-M1 Manuscript Reconstruction Contract

**Status:** frozen preparation contract
**Scientific training authorized:** no
**Source evidence commit:** `1680b21`

## 1. Objective

Build a new Chinese manuscript around the frozen relay-failure topology-robustness evidence. This stage converts existing evidence into a traceable paper; it does not create new scientific evidence or change any historical decision.

## 2. Immutable scientific position

The manuscript shall present DRTP-SG-MAPPO as an architecture-preserving adaptive topology-perturbation training method with positive historical mean and median robustness effects and non-negligible training-seed sensitivity.

The following remain immutable:

- `DRTP_Q2_LIMITATION_ONLY`;
- the historical development `NO-GO`;
- the held-out `FAIL`;
- development seed1902 and held-out seed2002;
- mixed safety outcomes;
- the 3-UAV simulation scope;
- `E2 — NO_FAIR_EXTERNAL_COMPARATOR`.

## 3. Primary comparison and ablation

The main-paper causal design comparison is:

`UTR-SG-MAPPO versus DRTP-SG-MAPPO`.

Both methods share the 116,728-parameter Single-Graph MAPPO architecture, PPO, seven topology-condition groups, 50% nominal anchor, environment, reward, actor information boundary, training budget within each stratum, and evaluation protocol. The intended difference is uniform versus bounded adaptive failure-group weighting.

This comparison must appear in the main paper. It shall not be described as universal proof that adaptive weighting is beneficial.

## 4. Evidence hierarchy

1. Frozen task and topology/path mechanism reports.
2. Absolute contract-stratified results.
3. Paired training-seed effects and reliability statistics.
4. Safety and exposure-validity evidence.
5. Mechanism telemetry.
6. Related-work positioning.

Development 3M and held-out 10M are separate strata. A five-pair cross-stratum summary is descriptive only.

## 5. Legacy-content policy

`paper_latex_3d_en/` is a legacy recovery/EA-RG manuscript and is not a source of DRTP performance claims. It may be consulted only for generic environment prose after line-by-line provenance review.

The following legacy content is prohibited in the reconstructed manuscript:

- EA-RG-MAPPO-S as the final method;
- Multi-Relation Full, Role-Gate, Gate Prior, or union-residual claims;
- recovery rate, RMST, recovery-time Pareto, and legacy success claims;
- information-blackout or information-restoration framing;
- legacy HAPPO/MAPPO tables whose contracts are not comparable to the DRTP evidence.

## 6. Writing order

1. research canon and terminology ledger;
2. claim–evidence and paragraph argument maps;
3. Methods and Experimental Protocol;
4. Results and Reliability;
5. Discussion and Conclusion;
6. Introduction and Related Work;
7. Abstract and Title;
8. final claim, citation, figure-source, and terminology audit.

## 7. Stop rules

No new algorithm, seed rescue, external comparator training, canonical run, OOD expansion, scalability experiment, HIL, or real-flight test may be started under M1.

If a paragraph requires evidence outside the frozen canon, insert `[EVIDENCE GAP]` and remove or narrow the claim. Do not launch an experiment from the manuscript-writing stage.
