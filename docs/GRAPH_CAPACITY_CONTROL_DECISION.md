# Graph capacity-control decision

**Pre-result decision: Option B.** Retain the ordinary Single-Graph comparator for the representation comparison and preregister/prepare a parameter-matched Single-Graph comparator if the parameter audit shows a difference larger than approximately 5%.

The canonical four-method identity remains unchanged for Phase 3A until the parameter-count script and method manifest are completed. No capacity decision has been made from performance results. A parameter-matched comparator, if added, requires a separate protocol amendment before training.
