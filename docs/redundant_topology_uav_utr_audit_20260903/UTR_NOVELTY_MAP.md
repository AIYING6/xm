# UTR novelty map

```text
                         Existing generic families
         domain randomization | PLR/curricula | robust objectives | comm-MARL
                  \                  |                 |             /
                   \                 |                 |            /
                    -------- UTR = equal exposure over frozen groups --------
                                      |
                      No generic algorithmic novelty
                                      |
       topology-failure benchmark: legal fault support + recoverability classes
                                      |
                  possible publication contribution, pending evidence
```

The map deliberately separates a known generic operation—uniformly drawing training
conditions—from the potentially more distinctive research object: a topology-failure benchmark
where conditions are legally executable, physically interpretable, and non-degenerate under a
role-structured communication graph.

