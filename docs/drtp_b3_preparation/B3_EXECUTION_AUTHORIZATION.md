# B3 execution authorization

Author authorization received: execute only original UTR/DRTP on paired seeds
2701–2703, exactly 1,000,192 environment steps each, with maximum safe
concurrency six.

The authorized run is cloud-only. It preserves every 0.25M milestone and all
read-only telemetry, performs no early stop, replacement, performance-driven
rerun, 3M extension, or stabilization modification. After six successful
runs, it evaluates the frozen B3 development tape and generates only the B3
1M mechanism-gate data product.

The delivery wrapper records the frozen scientific source commit
`69117b628fe85026e0638abb73dcfbfb04e4a64c`; it makes no algorithmic change.
