# EDR-Q2 — Implementation and Development Contract (Design Only)

## Current authority

This is prospective design only. It authorizes no implementation, tape, training, rollout, held-out evaluation, or canonical evaluation.

## Frozen future boundary

- Implement only the two EDR aggregation substitutions in the dossier.
- Keep the SG actor/critic, 116,728 parameters, PPO, environment, reward, failure semantics, actor boundary, and fixed UTR topology exposure unchanged.
- Add no memory, support/sensitivity loss, uncertainty, MoE, Transformer, curriculum, adaptive sampler, return feedback, or optimizer modification.
- Before training, pass parameter equality, actor-boundary and graph-legality regression, deletion-locality algebra, deterministic replay, save/reload continuation, logging invariance, and one-update finite-value smoke.

## Future development contract

Subject to provenance confirmation, reuse T1 UTR as the paired reference for development seeds `2201–2205`. EDR alone would train from scratch for the same frozen mature budget and exposure contract; final checkpoints only; no checkpoint promotion, seed exclusion, budget extension, or hyperparameter sweep.

To continue beyond development, EDR must show no catastrophic seed, nominal retention, collision/constraint safety, lower timeout, and favorable F0/OOD mean/OOD worst direction in at least 4/5 paired seeds. Failure closes EDR and does not authorize another candidate.
