# P3-P2 training authorization

**Status:** `P3_P2_TRAINING_AUTHORIZED`

Authorization covers only the frozen fresh-seed pilot defined in
`P3_P2_FRESH_PILOT_CONTRACT.md`. It permits cloud execution of the ten specified
trajectories and their fixed post-training development evaluation. It does not
authorize a new algorithm, independent replication, longer training, parameter
changes, or automatic continuation.
